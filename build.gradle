group 'BjsGroupId'
version '0.1-SNAPSHOT'

subprojects {
    group 'BjsGroupId'

    apply plugin: 'java'
    apply plugin: 'jacoco'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}